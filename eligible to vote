#include <iostream>
string voteAge(int age)
{
  string str = "";
  if(age >= 18)
    str = "Eligible for Voting! ";
  else
    str = "not eligible for Voting! ";
	return str;
}
 
int main()
{
	int age;
	cout<<"\nEnter age";
	cin>>age;
 
  cout << voteAge(age) << endl;
  
	return 0;
}
